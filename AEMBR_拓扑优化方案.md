# AEMBR 拓扑优化方案：稀疏 → 知识结构 → 元知识结构

**袭明 · 深渊实验室** | 2026-08-10

---

## 直觉 1：稀疏拓扑 → 实际知识结构拓扑

**当前问题**：BERT 余弦相似度是"无差别的语义邻近度"。`细胞` ↔ `生物学` 和 `细胞` ↔ `监狱`（牢房）都是高余弦——因为它们共享上下文，但前者是知识关联，后者是歧义。

**目标**：把无差别的语义相似度，升级为**分层的知识结构图**。

### 优化点

| # | 优化 | 方法 | 效果 |
|---|---|---|---|
| **O1** | 领域聚类 | 对 500 词做 BERT 嵌入的层次聚类 → 标注领域标签 | 同域词耦合增强·跨域耦合衰减 |
| **O2** | 词性标注加权 | jieba 词性标注（名词/动词/形容词/连词）施加类型偏好 | 名词↔名词增强·动词↔名词保持·连词全局弱耦合 |
| **O3** | 共现强化 | PMI 共现值作为余弦相似度的贝叶斯先验 | 消除纯上下文噪音（如 "苹果·手机" vs "苹果·水果"） |
| **O4** | 上下位关系注入 | WordNet/HowNet 的上位词下位词关系 → 非对称耦合 | `狗 → 动物` 单向强耦合，`动物 → 狗` 弱耦合 |
| **O5** | 因果/功能关系标注 | 规则模板提取（"X 导致 Y""X 是 Y 的一种""X 用于 Y"） | 非对称有向边·方向编码推理逻辑 |
| **O6** | 歧义消解 | 对多义词生成独立节点（`苹果[水果]` `苹果[公司]`） | 每个义项独立拓扑·上下文选择子图 |
| **O7** | 稀疏度优化 | 根据节点"信息含量"分配不同的出度数（核心概念多边·边缘概念少边） | 信息瓶颈更合理地分配在拓扑中 |

### O1：领域层次聚类

```python
# 伪代码
def domain_cluster(embeddings, vocab, n_domains=20):
    from sklearn.cluster import AgglomerativeClustering
    labels = AgglomerativeClustering(n_clusters=n_domains).fit_predict(embeddings)
    domain_map = {w: f"domain_{l}" for w, l in zip(vocab, labels)}
    return domain_map

# 权重修正：同域增强 1.3x·跨域衰减 0.5x
def adjust_by_domain(J, vocab, domain_map):
    for i in range(len(vocab)):
        for j in range(i+1, len(vocab)):
            if domain_map[vocab[i]] == domain_map[vocab[j]]:
                J[i,j] *= 1.3  # 同域增强
            else:
                J[i,j] *= 0.5  # 跨域衰减
```

### 预期收益

- 消除"苹果·水果"和"苹果·手机"的混淆
- 知识检索的精度提升 2-3 倍
- 推理路径更短（同域内跳数更少）

---

## 直觉 2：知识结构拓扑 → 元知识结构拓扑

**当前问题**：500 个词是"对象级"的——`量子`、`计算`、`纠错`、`算法`。但它们之间的**关系类型**没有被编码进拓扑。

**目标**：在对象词之上，引入**元概念节点**——约束类型、解法策略、学科范式、推理模式。这些元节点不直接对应输入词，而是作为**拓扑的结构骨架**，引导推理的方向。

### 优化点

| # | 优化 | 方法 | 效果 |
|---|---|---|---|
| **O8** | 约束类型节点 | 从裂变公理库提取：时序同步、空间对齐、信息退化、不可传递性… | 输入词先映射到约束类型·再沿约束→解法边推理 |
| **O9** | 解法策略节点 | 从裂变公理库提取：概率建模、拓扑约束、反馈控制、统计推断… | 约束→解法的天然有向边 |
| **O10** | 学科范式节点 | 从文澜阁分类标签提取：分布式、密码学、量子、生物学… | 领域切换时的路由节点 |
| **O11** | 因果链模式 | "X 引发 Y""X 阻碍 Y""X 与 Y 协同"三条元边 | 逻辑推理的骨架 |
| **O12** | 推理步模式 | "观察→假设→验证→结论"的时序模式 | 多步推理的跳板 |

### O8-O9：约束→解法元拓扑

```
约束类型节点（~20 个）         解法策略节点（~15 个）
  时序同步    ──────→  概率建模
  空间对齐    ──────→  拓扑约束  
  信息退化    ──────→  反馈控制
  不可传递性  ──────→  统计推断
  闭合判定    ──────→  凸优化
  ...
```

这些元节点以**桥接边**连接对象词：

```
"量子纠缠" → 信息退化 → 概率建模 → "量子纠错"
"分布系统" → 时序同步 → 反馈控制 → "共识协议"
```

### O10：学科范式路由

```
输入: "量子计算的纠错问题"
  → 路由到 学科=量子 子图
  → 激活 约束类型=信息退化
  → 触发 解法策略=拓扑约束
  → 扩散到 对象节点=量子纠错·表面码·逻辑比特
```

### 预期收益

- 跨领域跳跃的精度（裂变工厂的核心能力被编码进拓扑本身）
- 推理可解释性——每条激活路径有明确的类型标注
- 拓扑的迁移性——通用元节点可被不同学科的对象拓扑复用

---

## 三层拓扑架构

```
Layer 3: 元知识拓扑（~40 节点）
  约束类型 · 解法策略 · 学科范式 · 因果链 · 推理步
      ↓ 桥接边（稀疏·有向·非对称）
Layer 2: 知识结构拓扑（~500 节点）
  领域聚类 · 词性加权 · 上下位 · 因果关系
      ↓ 密集边（余弦 + PMI·对称）
Layer 1: 语义拓扑（原始 BERT）
  500 词的原始余弦相似度
```

**每层的作用**：
- L1：语义基础——"这些词在语言里是相关的"
- L2：知识组织——"这些词在知识体系里是这样关联的"
- L3：推理引导——"面对这类约束，应该用这类解法"

---

## 实施优先级

| 优先级 | 优化号 | 预估收益 | 实施难度 | 耗时 |
|---|---|---|---|---|
| 🔴 P0 | O1 领域聚类 | 立即提升解码精度 | 低 | 1h |
| 🔴 P0 | O7 度优化 | 显著降低噪声边 | 低 | 0.5h |
| 🟡 P1 | O3 共现强化 | 消除上下文歧义 | 中 | 2h |
| 🟡 P1 | O4 上下位关系 | 引入推理方向性 | 中 | 3h（需要 HowNet） |
| 🟢 P2 | O8-O10 元节点 | 根本性架构升级 | 中 | 4h |
| 🟢 P2 | O5 因果关系 | 引入因果推理 | 中 | 3h |
| ⚪ P3 | O6 歧义消解 | 多义词处理 | 高 | 1d |
| ⚪ P3 | O11-O12 推理模式 | 多步推理 | 高 | 1d |

---

## 最小可行验证（今天可做）

1. **O1 领域聚类**：对现有 500 词 BERT 嵌入做层次聚类 → 20 个领域 → 同域增强 1.3x → 重跑推理 → 对比解码精度
2. **O7 度优化**：按词频赋予差异化出度（高频词 30 边·低频词 10 边）→ 重跑 → 对比收敛速度
3. **O8 元节点注入（手动版）**：手动定义 10 个约束类型 + 10 个解法策略 → 注入到 500 词拓扑中 → 推理"分布式系统共识" → 观察激活路径是否经过正确的元节点

**如果 O1+O7 验证通过** → 立即进入 O8-O10 的自动化管道。
